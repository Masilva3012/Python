# Escreva um programa que leia um valor em metros e o exiba convertido em centimetros e milímetros

medida = float(input('Uma distancia em Metros: '))
km = medida / 1000
hm = medida / 100
dam = medida / 10
dm = medida * 10
cm = medida * 100
mm = medida * 1000
print('A media de {}m corresponde a {}km, {}hm, {}dam, {}dm {}cm e {}mm'.format(medida, km, hm, dam, dm, cm, mm))
